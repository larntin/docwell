# 项目知识归一化约定（最高优先级）

> 目的：让所有项目知识**唯一权威、可被发现、不腐烂**。任何 AI session、任何新加入的协作者读到这一段，都应该知道去哪查、往哪写、什么不能写。
>
> 使用方式：把本文件整段粘进项目根 `CLAUDE.md`（或 `AGENTS.md`）顶部。本约定只管三处——`docs/` 文档、OpenSpec 工作区、Superpowers 工作区；按项目情况调整第 5 节的例外清单即可。项目若无 UI 协作，第 6 节整节删除。

## 1. 权威位置总览

本项目的持久知识**只**存在这几个地方，其它位置出现的 `*.md` 一律视为违规：

- `docs/` —— 所有业务、架构、约定、已决方案、排错记录的唯一权威。以 `${NNN}_`（`001_`、`002_`…）编号的**扁平** `.md` 文件组织，**不建子目录**。UI 设计稿（`.html`）同样平铺于此，命名见第 6 节。
- 根 `CLAUDE.md` / `AGENTS.md` —— 仅收录“最高优先级、读任何文件前必须先知道”的元规则，不放具体业务知识。
- 每个模块/包/skill 目录下的 `README.md`（描述该模块对外契约，不复述全局知识）。

任何实现、排错、决策前，**先在 `docs/` 中检索**。

## 2. docs/ 组织原则

- **扁平编号**：所有文档平铺在 `docs/` 根下，用 `${NNN}_` 前缀管理阅读顺序。**编号一律三位数**（`001_`、`002_`…），禁止 `00_`、`1_` 这类写法。**不建子目录**——文档的领域/类型用文件名表达，例如 `001_architecture.md`、`002_conventions.md`、`003_decisions.md`、`004_troubleshooting.md`。
- **号段划分**：`001`–`109` 给 `.md` 文档，编号后接**下划线**（`001_architecture.md`）；`110` 起留给 UI 设计稿（`.html`），编号后接**连字符**（`110-登录流程交互-…`，见第 6 节）。两类同样平铺、同样不建子目录。
- **唯一权威**：同一主题只允许一篇权威文档。发现重复立即合并，其它位置改为链接。
- **写前必查**：用 glob/grep 按文件名和标题搜，不要逐个 `cat`；命中 ≤3 才全文读。优先复用/扩展已有文件，而非新建。
- **简明、链接优先**：文档只写必要、简明、无歧义的内容。背景只写读懂本文必须的部分，其它链到对应权威文档。不复述 `CLAUDE.md` 或相邻文档已有的规则。
- **推荐 YAML frontmatter**（便于后续搜索）：`title`、`tags`、`module`、`status`（active / deprecated）、`last_updated`。
- **编号冲突处理**：多分支并行写文档若撞号，后合并者负责重排并同步更新所有引用链接。UI 设计稿同此规则。
- **过时处理**：修改代码/架构若影响既有文档，同一次改动内必须同步更新；无法即时更新时在文档头部标 `deprecated: <原因，指向替代文档>`。禁止留下与实现不符的文档。

## 3. 中间产物分层（三层模型）

知识产物按**寿命**分三层，各有专门位置：

**第 1 层 —— durable（权威沉淀）**
进 `docs/`。判定：任务结束后仍长期有效，未来 session 会反复查阅。UI 设计稿（第 6 节）属于本层。

**第 2 层 —— working（阶段性工作产物）**
进仓库的**专用工作区目录**，随任务生命周期存在（活跃 → 归档 → 清理）。工作区由具体 skill/workflow 托管，本项目登记两个：

- `openspec/changes/<change-id>/`、`openspec/specs/` —— OpenSpec 技能的提案/规约工作区。
- `superpowers/`（`specs/`、`plans/`、`notes/` 等骨架目录）—— Superpowers 技能工作区。

规则：
- 工作区产物**不是项目知识**，不得被其它模块当作权威引用。要被长期引用的结论，必须提炼进 `docs/`（第 1 层）。
- 工作区目录由对应 skill 托管；AI 不得在工作区外另建平行的 `plan.md` / `analysis.md`。
- 任务完成后若工作区内容已无用，及时清理或归档，不要堆积。

**第 3 层 —— ephemeral（一次性中间产物）**
进项目根的 `.tmp/` 目录（建议按 `.tmp/<run-id>/` 分隔）。判定：当前 session 消化掉即可，另一个 session 不会来读。典型场景：单次 grep 结果缓存、subagent 间传递的中间文本、截图/录屏、临时构建产物。
- 首次写入 `.tmp/` 前先检测它是否已被 `.gitignore` 忽略；若未忽略，**征求用户同意后**把 `.tmp/` 加入 `.gitignore`。这些产物**绝不进版本库**。

**分界判定**：
- 有 skill/workflow 托管 → 第 2 层（进仓库工作区）
- 没有 skill 托管、只是 AI 自己想记一下 → 第 3 层（项目 `.tmp/`）
- 任务结束后还会被反复查阅 → 第 1 层（`docs/`）

## 4. 禁止事项

- 禁止在任意位置散写项目知识（`notes.md`、`readme2.md`、`plan.md`、`analysis.md`、临时笔记等）。
- 禁止在 `docs/` 目录外新建 `*.md` 文档解释业务/架构概念。**例外需在本规约中显式登记**，未登记视为违规。
- 禁止在 `docs/` 下建子目录拆分文档；一律用 `${NNN}_` 三位编号的扁平文件，类型/领域写进文件名。
- 禁止在多处复制粘贴同一段说明。只允许一处权威 + 其它地方链接。
- 禁止把没有 skill/workflow 托管的中间产物留进版本库。若使用 OpenSpec / Superpowers 等 skill，产物只能落在其**登记的工作区目录**；AI 自发的 `plan.md` / `analysis.md` / `notes.md` 若不属于任何已登记工作区，一律去项目 `.tmp/`（已 gitignore，不进版本库）。
- 禁止在代码改动时“只改代码不动文档”，若对应权威文档被影响。
- 禁止为同一份 UI 设计稿保留多个版本文件；调整一律改名递增，历史归 git（见第 6 节）。
- 禁止跳过交互稿直接做视觉稿。

## 5. 登记的例外

> 新增例外时在本节追加一行并注明原因。未登记的例外视为违规。

- `CLAUDE.md` / `AGENTS.md` —— 根目录元规则入口。
- `<module>/README.md` —— 模块对外契约。
- `<skill>/SKILL.md`、`<agent>/*.agent.md` —— 插件/agent 组件自带规范。
- `_dev_appendix.md` —— 研发调试临时笔记（如项目使用）。
- `openspec/changes/<id>/`、`openspec/specs/` —— OpenSpec 技能工作区（如项目使用）。
- `superpowers/`（`specs/`、`plans/`、`notes/`）—— Superpowers 技能工作区（如项目使用）。
- `docs/${NNN}-*.html` —— UI 设计稿，`docs/` 内唯一允许的非 `.md` 文件（如项目使用，见第 6 节）。

## 6. UI 设计稿留痕

> 面向多 AI 同事协作的 UI 设计。项目若无 UI 协作，整节删除。

**两类稿，先交互后视觉，不可颠倒：**

- **交互稿** —— 只讲功能与流程：有哪些元素、点了会怎样、状态如何流转、边界与异常。允许用最简单的框线元素示意布局以达成共识；**禁止**定颜色、字体、精确比例。
- **视觉稿** —— 对应交互稿**定稿之后**才开始：配色、控件样式、字号、间距与比例、状态样式。

**命名**（放 `docs/` 根，与 `${NNN}_` 文档平铺）：

```
${NNN}-${名称}-${版本}-${YYYY-MM-DD}-${agent名称}.html
```

- `NNN` —— 三位编号，从 `110` 起。同一功能的交互稿与视觉稿用**相邻编号成对**（`110` 交互 / `111` 视觉），下一功能接 `112`/`113`。
- `名称` —— 中文短名，**带“交互”/“视觉”字样**以便肉眼区分，如 `登录流程交互`。
- `版本` —— `n.n`，从 `1.0` 起。每次调整小版本 +1，两段均无进位上限（`20.22` 合法）；**大版本由人指定**，AI 不得自行升大版本。
- `YYYY-MM-DD` —— 本版产生的日期。
- `agent名称` —— 本版的**最后修改者**，小写（`claude`、`codex`、`gemini`…）。
- **定稿** —— 版本号前直接加“定稿”二字。

示例：

```
docs/110-登录流程交互-定稿1.4-2026-08-23-claude.html   # 交互稿，已定稿
docs/111-登录流程视觉-2.0-2026-08-25-claude.html       # 对应视觉稿，迭代中
docs/112-订单列表交互-1.1-2026-08-26-codex.html        # 下一个功能接 112/113
```

同一份稿在目录里**永远只出现一行**；上面三行是三份不同的稿，不是同一份稿的三个版本。

**迭代规则（关键，别搞反）：**

- 一份稿在任何时刻**只有一个文件**。调整 = `git mv` 改名（版本号、日期、agent 名一起更新），**不新建并存文件，不保留旧文件**。同一内容禁止存在两个文件。
- 留痕靠**文件名**（谁、何时、第几版）+ **git 历史**，不靠堆积历史文件。用 `git log --follow` 回溯演进。
- 任何 agent 都可接手任何稿；接手后把 `agent名称` 换成自己，版本号在原基础上**继续递增**，不另起版本线。
- 定稿后若需再改：去掉“定稿”二字、小版本继续递增；重新定稿时再加回（`定稿1.4` → `1.5` → `定稿1.6`）。
- HTML 必须**自包含单文件**：不引外部 CSS/JS/字体/图片 CDN，保证任何同事任何机器直接打开即可读。
