<!-- docwell v0.2 -->

# 项目知识归一化约定（最高优先级）

> 目的：让所有项目知识**唯一权威、可被发现、不腐烂**。任何 AI session、任何新加入的协作者读到这一段，都应该知道去哪查、往哪写、什么不能写。
>
> 使用方式：把本文件整段粘进项目根 `CLAUDE.md`（或 `AGENTS.md`）顶部，然后**只改第 5 节**——把本项目实际使用的 skill/workflow 工作区登记进去。其余各节通用，不必按项目改动。

## 1. 起手式

**任何任务开始前，先读 `docs/000_index.md`。** 索引没覆盖到的，再用 glob/grep 按文件名和标题搜 `docs/`，命中 ≤3 篇才全文读。检索完成之前，不要开始实现、排错或新建文档。

## 2. 权威位置总览

本项目的持久知识**只**存在这几个地方，其它位置出现的 `*.md` 一律视为违规：

- `docs/` —— 所有业务、架构、约定、已决方案、排错记录的唯一权威。以 `${NNN}_` 三位编号的**扁平** `.md` 文件组织，**不建子目录**。
- 根 `CLAUDE.md` / `AGENTS.md` —— 仅收录“最高优先级、读任何文件前必须先知道”的元规则，不放具体业务知识。
- 每个模块/包/skill 目录下的 `README.md`（描述该模块对外契约，不复述全局知识）。

## 3. docs/ 组织原则

- **扁平编号**：所有文档平铺在 `docs/` 根下，用 `${NNN}_` 前缀管理阅读顺序。编号**一律三位数**（`000_`、`001_`…），禁止 `00_`、`1_` 这类写法；分隔符一律下划线。**不建子目录**——领域/类型写进文件名，例如 `001_architecture.md`、`002_conventions.md`、`003_decisions.md`、`004_troubleshooting.md`。
- **`000_index.md` 是索引**：固定占用 `000`，一行一篇，写编号、标题、一句话说明。**新增、删除、重命名任何文档，必须在同一次改动内更新索引。** 索引是“可被发现”的唯一保障，扁平目录一旦超过几十篇，只靠 grep 必然漏。
- **编号只递增，永不重排**：新增文档取当前最大编号 +1。编号只表示大致先后，**不承诺连续**。多分支并行撞号时，后合并者改用下一个未占用编号——**已存在文档的编号永不变更**。重排会让所有外部引用（代码注释、commit message、其它文档、AI 的既有记忆）集体失效，代价远大于编号连续的收益。
- **唯一权威**：同一主题只允许一篇权威文档。发现重复立即合并，其它位置改为链接。
- **只写代码里读不出来的东西**：`docs/` 记录的是**为什么这么选、踩过什么坑、有哪些外部约束、达成了什么口头约定**。函数签名、目录结构、依赖清单、配置项列表等能直接 `cat` / `grep` 出来的事实**一律不写**——这类内容腐烂最快、误导性最强，是“文档比没文档更糟”的主要来源。
- **写前必查**：优先复用或扩展已有文件，而非新建。
- **简明、链接优先**：只写必要、无歧义的内容。背景只写读懂本文所必需的部分，其余链到对应权威文档；不复述 `CLAUDE.md` 或相邻文档已有的规则。
- **推荐 YAML frontmatter**（便于后续搜索）：`title`、`tags`、`module`、`status`（active / deprecated）、`last_updated`。
- **过时必处理**：修改代码/架构若影响既有文档，同一次改动内必须同步更新。实在无法即时更新，在文档头部标 `deprecated: <原因，指向替代文档>`。`deprecated` 只是**过渡态**：必须带替代文档链接，并在下一次触及同模块的改动中删除，不允许长期堆积。

## 4. 中间产物分层（三层模型）

知识产物按**寿命**分三层，各有专门位置：

**第 1 层 —— durable（权威沉淀）**
进 `docs/`。判定：任务结束后仍长期有效，未来 session 会反复查阅。

**第 2 层 —— working（阶段性工作产物）**
进仓库内**第 5 节已登记**的 skill/workflow 工作区目录，随任务生命周期存在（活跃 → 归档 → 清理）。

- 工作区产物**不是项目知识**，不得被其它模块当作权威引用。要被长期引用的结论，必须提炼进 `docs/`（第 1 层）。
- 工作区目录由对应 skill 托管；AI 不得在工作区外另建平行的 `plan.md` / `analysis.md`。
- **未在第 5 节登记的目录，不是工作区。**
- 任务完成后工作区内容已无用的，及时清理或归档，不要堆积。

**第 3 层 —— ephemeral（一次性中间产物）**
进项目根的 `.tmp/`（建议按 `.tmp/<run-id>/` 分隔）。判定：当前 session 消化掉即可，另一个 session 不会来读。典型场景：单次 grep 结果缓存、subagent 间传递的中间文本、截图/录屏、临时构建产物。
`.tmp/` 必须已在 `.gitignore` 内，否则不得写入。这些产物**绝不进版本库**。

**分界判定**：

- 有第 5 节登记的 skill/workflow 托管 → 第 2 层（进仓库工作区）
- 无人托管、只是 AI 自己想记一下 → 第 3 层（`.tmp/`）
- 任务结束后还会被反复查阅 → 第 1 层（`docs/`）

## 5. 登记清单

> **本规约唯一需要按项目修改的一节。** 新增条目时追加一行并注明用途；未登记即违规。

**已登记的工作区** —— 第 2 层产物的合法落点，按本项目实际使用增删：

- `openspec/changes/<change-id>/`、`openspec/specs/` —— OpenSpec 技能的提案/规约工作区。
- `superpowers/`（`specs/`、`plans/`、`notes/` 等骨架目录）—— Superpowers 技能工作区。

**已登记的 `docs/` 外文档例外**：

- `CLAUDE.md` / `AGENTS.md` —— 根目录元规则入口。
- `<module>/README.md` —— 模块对外契约。
- `<skill>/SKILL.md`、`<agent>/*.agent.md` —— 插件/agent 组件自带规范。

## 6. 红线速查

> 以下是前几节规则的负面复述，便于扫读。违反任何一条即为违规。

- 禁止在 `docs/` 与第 5 节登记位置之外新建 `*.md` 解释业务或架构（`notes.md`、`readme2.md`、`plan.md`、`analysis.md`、临时笔记等）。
- 禁止在 `docs/` 下建子目录。
- 禁止变更已存在文档的编号。
- 禁止把代码或配置能直接读出的事实写进 `docs/`。
- 禁止在多处复制粘贴同一段说明——一处权威，其余链接。
- 禁止把无人托管的中间产物留进版本库；不属于任何已登记工作区的，一律去 `.tmp/`。
- 禁止改代码时不同步受影响的权威文档。
